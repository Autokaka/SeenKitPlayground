{
  "type": "module",
  "name": "playground",
  "version": "1.0.0",
  "scripts": {
    "bundle": "cd build && zip demo.seen main.wasm && cd ..",
    "install": "mv build/demo.seen /Users/luao/Documents/CXX/SeenKit/example/res",
    "compile": "asc assembly/main.ts --target main --use abort= --use trace= --use seed=",
    "main": "npm run compile && npm run bundle && npm run install",
    "dump": "wasm-objdump -j Import -x build/main.wasm"
  },
  "devDependencies": {
    "assemblyscript": "latest"
  }
}
